\documentclass[$if(fontsize)$$fontsize$,$endif$$if(handout)$handout,$endif$$if(aspectratio)$aspectratio=$aspectratio$,$endif$]{beamer}

% load packages and helper commands
\input{theme/packages}
\input{theme/commands}

% Uni BI font (needs LaTeX engine xelatex or lualatex)
$if(unibi-font)$
\usepackage[no-math]{fontspec}
\setmainfont[
    Path            = ./theme/,
    Extension       = .otf,
    Ligatures       = TeX,
    UprightFont     = *-Regular,
    ItalicFont      = *-RegularItalic,
    BoldFont        = *-Bold,
    BoldItalicFont  = *-BoldItalic
]{Lelo}
$endif$

% Eco Uni BI beamer style
\usepackage{theme/eco_bielefeld}

% for code blocks
$if(highlighting-macros)$
  $highlighting-macros$
$endif$ 

% metadata fields
$if(title)$
\title$if(short-title)$[$short-title$]$endif${$title$}
$endif$

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

$if(author)$
\author{$for(author)$\texorpdfstring{$author$}{$author$}$sep$ \and $endfor$}
$endif$

$if(date)$
\date{$date$}
$endif$

$if(institute)$
\institute{$for(institute)$$institute$$sep$ \and $endfor$}
$endif$

% section behavior
$if(toc)$
$if(toc-title)$
\newcommand{\biemtocname}{$toc-title$}
$else$
\newcommand{\biemtocname}{Outline}
$endif$

\AtBeginSection[]{
  \begin{frame}
    \frametitle{\biemtocname}
    \tableofcontents[currentsection]
  \end{frame}
}
$endif$

% start content
\begin{document}

$if(title)$
\begin{frame}[plain]{}
  \titlepage
\end{frame}
$endif$

$if(toc)$
\begin{frame}
  \frametitle{\biemtocname}
  \tableofcontents
\end{frame}
$endif$

$body$

$if(last-slide)$
\begin{frame}[fragile]{}
  \vspace{0.5cm}

  \begin{center}
    {\Large $if(last-slide-message)$$last-slide-message$$else$Thanks for your attention!$endif$}
  \end{center}

  \vfill

$if(last-slide-email)$
  \begin{itemize}
    \item[{\protect\faEnvelope}] $last-slide-email$
$if(last-slide-website)$
    \item[{\protect\faLaptop}] $last-slide-website$
$endif$
  \end{itemize}
$else$
$if(last-slide-website)$
  \begin{itemize}
    \item[{\protect\faLaptop}] $last-slide-website$
  \end{itemize}
$endif$
$endif$

\end{frame}
$endif$

\end{document}
