% Keep class options on one logical line so pandoc conditionals do not
% introduce paragraph breaks in \documentclass options.
\documentclass[$if(fontsize)$$fontsize$,$endif$$if(handout)$handout,$endif$$if(aspectratio)$aspectratio=$aspectratio$,$endif$]{beamer}

% Core packages and local helper commands
\input{theme/packages}
\input{theme/commands}

% Optional university font (requires LuaLaTeX or XeLaTeX)
$if(unibi-font)$
\usepackage[no-math]{fontspec}
\setmainfont[
    Path            = ./theme/,
    Extension       = .otf,
    Ligatures       = TeX,
    UprightFont     = *-Regular,
    ItalicFont      = *-RegularItalic,
    BoldFont        = *-Bold,
    BoldItalicFont  = *-BoldItalic
]{Lelo}
$endif$

% Optional local Beamer style
$if(eco-bielefeld)$
\usepackage{theme/eco_bielefeld}
$endif$

% for code blocks
$if(highlighting-macros)$
  $highlighting-macros$
$endif$ 

% Optional emoji support
$if(emoji)$
\usepackage{emoji}
$endif$

% Pandoc helper for tight markdown lists
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Metadata fields (all optional)
$if(title)$
\title{$title$}
$endif$

$if(subtitle)$
\makeatletter
\providecommand{\subtitle}[1]{%
  % Add subtitle support even if the loaded theme does not define \subtitle
  \apptocmd{\@title}{\par {\large #1 \par}}{}{}
}
\makeatother
\subtitle{$subtitle$}
$endif$

$if(author)$
\author{$for(author)$\texorpdfstring{$author$}{$author$}$sep$ \and $endfor$}
$endif$

$if(date)$
\date{$date$}
$endif$

$if(institute)$
\institute{$for(institute)$$institute$$sep$ \and $endfor$}
$endif$

% ---- Navigation helpers ----------------------------------------------------
% Use with labels created by \hypertarget{<label>}{} inside a frame.
\newcommand{\appendixbutton}[2]{\hyperlink{#1}{\beamerbutton{#2}}}
\newcommand{\returnbutton}[2]{\hyperlink{#1}{\beamergotobutton{#2}}}

% Slightly rounder button shapes for a friendlier look
% (single optional argument keeps compatibility across beamer versions)
\setbeamertemplate{button}[rounded]

% ---- Section / TOC behavior ------------------------------------------------
$if(toc)$
$if(toc-title)$
\newcommand{\biemtocname}{$toc-title$}
$else$
\newcommand{\biemtocname}{Outline}
$endif$

\AtBeginSection[]{
  \begin{frame}
    \frametitle{\biemtocname}
    \tableofcontents[currentsection]
  \end{frame}
}
$endif$

$if(last-slide)$
\usepackage{fontawesome}
$endif$

\begin{document}

$if(title)$
\begin{frame}[plain]{}
  \titlepage
\end{frame}
$endif$

$if(toc)$
\begin{frame}
  \frametitle{\biemtocname}
  \tableofcontents
\end{frame}
$endif$

$body$

$if(last-slide)$
\begin{frame}{}
  \vspace{0.5cm}
  \begin{center}
    {\Large $if(last-slide-title)$$last-slide-title$$else$Thanks for your attention!$endif$}
  \end{center}
  \vfill
  \begin{itemize}
$if(last-slide-email)$
    \item[\faEnvelope] \href{mailto:$last-slide-email$}{$last-slide-email$}
$endif$
$if(last-slide-website)$
    \item[\faLaptop] \href{$last-slide-website$}{$last-slide-website$}
$endif$
  \end{itemize}
\end{frame}
$endif$

\end{document}
